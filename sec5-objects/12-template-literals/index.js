const another = `This is my
'first' message`; // template literals. use backticks instead of single or double quotes. allows us to create multi-line strings without using escape characters like \n. also allows us to embed expressions using ${expression}.

const name1 = 'John';

const another1 = 
`Hi ${name1} ${2 + 3},

Thank you for joining my mailing list.

Regards,
Mosh`;