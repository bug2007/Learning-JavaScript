const numbers = [1, 2, 3, 4];

// Adding elements
// numbers.push();
// numbers.unshift();
// numbers.splice();


// Removing elements
const last = numbers.pop(); // 4

const first = numbers.shift(); // 1

const numbers1 = [1, 2, 3, 4];
numbers1.splice(2, 2); // starting from index 2, delete 2 elements. result = [1, 2]
