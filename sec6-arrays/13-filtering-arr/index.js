const numbers = [1, -1, 2, 3];

const filteredArr = numbers.filter(function(value) {  // [1, 2, 3]
    return value >= 0;
});

// OR
// const filteredArr = numbers.filter(n => n >= 0);     // [1, 2, 3]






